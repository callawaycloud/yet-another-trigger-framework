{
    "name": "ccc_trigger_handler",
    "private": true,
    "version": "1.0.0",
    "description": "CCC Trigger Handler",
    "scripts": {
        "pretty-all-apex": "npx prettier --write 'src/**/*.{trigger,cls}'",
        "release": "sfdx force:package:version:create -p 'CCC Trigger Handler' -w 1000 -x"
    },
    "devDependencies": {
        "prettier": "1.19.1",
        "prettier-plugin-apex": "^1.0.0"
    }
}
